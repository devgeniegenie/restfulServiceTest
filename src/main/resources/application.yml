spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/restful?serverTimezone=UTC
    username: root
    password: 1a2a3a4a
  messages:
    basename: messages
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server:
  port: 8081

logging:
  level:
    org.springframework: info